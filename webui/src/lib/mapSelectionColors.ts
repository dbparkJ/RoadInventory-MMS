/**
 * High-contrast complementary accents reserved for the two selectable map
 * object families. Keeping them independent from track/layer colours makes a
 * selection recognisable even when many user-defined layers are visible.
 */
export const MAP_SELECTED_FRAME_COLOR = '#2dd4e8'
export const MAP_SELECTED_FEATURE_COLOR = '#ff6f3d'
